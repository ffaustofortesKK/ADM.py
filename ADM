# --- SEÇÃO 2: FILA DE PRESTADORES (COM DEPURADOR ATIVO) ---
st.subheader("🎤 Fila de Prestadores")

try:
    # Busca os dados
    prestadores = supabase.table("prestadores").select("*").execute().data
    
    if prestadores:
        # --- AQUI ESTÁ O TRUQUE ---
        st.write("Dados recebidos do Supabase (para depurar):")
        st.json(prestadores) # Isso vai mostrar TUDO o que o Supabase entregou
        
        # ... (seu restante código abaixo) ...
